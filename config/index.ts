import { AppInfo } from "../types";
import type { Metadata } from "next";

export const APP_INFO: AppInfo = {
    title: "mini-defender",
    description: "this is mini-defender WebApp",
    default_language: "en",
    copyright: "",
    privacy_policy: "",
}